# GitHub-Code-Reviewer
this system takes a git hub repositorty , analyzes it and shows the code health in chart form. It also indicates any security issues the code is facing and also gives the solution for it. A user can generate a PR(Pull Request) by generating a OAuth App in Github for automatic correction of code. The system will correct the code auto- in repository 
